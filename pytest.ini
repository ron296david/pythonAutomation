[pytest]
testpaths = tests
pythonpath = .
base_url = https://www.automationexercise.com
# For failed tests only, keep a Playwright trace (screenshots, actions, network) and a video, in test-results/
addopts = --tracing retain-on-failure --video retain-on-failure --output test-results
