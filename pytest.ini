[pytest]
testpaths = tests
pythonpath = .
base_url = https://www.automationexercise.com
# Keep a Playwright trace (screenshots, actions, network) for failed tests only, in test-results/
addopts = --tracing retain-on-failure --output test-results
